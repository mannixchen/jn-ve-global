export enum PreviewMode {
    /**
     * WPS 预览
     */
    WPS = 'wps',
    /**
     * 本地预览
     */
    LOCAL = 'local'
}

export enum PreviewType {
    IMG = 'img',
    PDF = 'pdf',
    DOCX = 'docx',
    EXCEL = 'excel',
    WPS = 'WPS'
}
